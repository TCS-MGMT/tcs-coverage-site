# TCS Coverage Scheduler — live site

Deploy-only copy of the app (5 static files). The source of truth, docs, tests,
and server code live in the private repo `TCS-MGMT/tcs-coverage-scheduler`.
Do not edit here — changes are pushed from the private repo via `deploy-site.sh`.
